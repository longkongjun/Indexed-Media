/// 纯函数、可解释的强规则识别决策。
pub mod decision;
/// 有界、规范化的证据值类型。
pub mod evidence;
/// 不可变人工复核决定与精确可复用反馈。
pub mod manual;
/// 有界身份线索与候选图值类型。
pub mod model;
/// 有界、无主动内容的 Kodi NFO 解析与能力式查找。
pub mod nfo;
/// organization 只读消费已核对身份的窄应用端口。
pub mod organization_port;
/// Unicode 文件名与目录身份解析。
pub mod parser;
/// 稳定的活动 `ReviewCase` 读模型。
pub mod review;
/// 已认证的处理与识别 REST 路由。
pub mod routes;
/// 识别用例协调边界。
pub mod service;
/// 事务化不可变识别存储。
pub mod store;

# Android

## 目标

提供 MediaFlow 的 Jetpack Compose 手机和平板正式 UI。

## 职责

Jetpack Compose 手机和平板正式 UI。

## 允许依赖

`packages/kmp-sdk`、`packages/ui-contract`、`packages/test-fixtures`、`contracts`。

## 非目标

Core 业务逻辑、TV UI、iOS UI、播放以及 `labs` 下的实验。

## 状态

未初始化

## 后续验证

`just check-android`

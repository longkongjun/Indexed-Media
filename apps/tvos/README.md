# tvOS

## 目标

提供 MediaFlow 的 SwiftUI 与 Focus Engine 浏览 UI。

## 职责

SwiftUI 与 Focus Engine 浏览 UI。

## 允许依赖

可选 `packages/kmp-sdk`、`packages/ui-contract`、`packages/test-fixtures`、`contracts`。

## 非目标

手机和平板 UI、Android UI、播放、Core 业务逻辑以及 `labs` 下的实验。

## 状态

未初始化

## 后续验证

`just check-tvos`

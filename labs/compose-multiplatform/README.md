# Compose Multiplatform 实验

## 目标

使用 Compose Multiplatform 验证一个 Android/iOS 学习垂直切片。

## 职责

一个使用 `packages/kmp-sdk` 的 Android/iOS 学习垂直切片。

## 允许依赖

`packages/kmp-sdk`、`contracts` 和实验局部代码。

## 非目标

完整功能等价、正式 TV 目标或正式客户端归属。

## 状态

未初始化

## 后续验证

`just check-compose-multiplatform`
